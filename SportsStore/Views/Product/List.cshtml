@model ProductsListViewModel
@foreach(var p in Model.Products)
{
    <div class="well">
        <h3><strong>@p.Name</strong>
        <span class="pull-right"
        </h3>
        @p.Description
        <h4>@p.Price.ToString("c")</h4>
    </div>
}
<div page-model="@Model.PagingInfo" page-action="List"></div>

